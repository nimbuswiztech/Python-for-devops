x = [1, 2, 3]
y = 5 

result = x is y

print(f"x is y: {result}")  # True, because both refer to the same object

# is not
a = "hello"
b = "world"

result = a is not b # True, because they are different objects

print(f"a is not b: {result}")  # True, because they are different objects